<%- include('../includes/head')%>
    <%- include('../includes/nav')%>
        <div class="row">
            <div class="col-2"></div>
            <div class="col-8">
                <h1>funcionarios</h1>
                <% if(funcionarios.length > 0) { %>
                    <ul>
                        <% funcionarios.forEach((funcionario) => { %>
                            <li><a href="#"><%= funcionario.nome %> </a></li>
                        <%})%>
                    </ul>

                <% }  else { %>
                    não existem funcionarios
                <% }%>
            <div class="col-2"></div>
        </div>
        <%- include('../includes/footer')%>